# gitinit
Project scaffold generator with templates for Python, Node, Go, CLI.
```bash
python gitinit.py myapp -t python
python gitinit.py api -t node -p ~/projects/api
python gitinit.py --list
```
## Zero dependencies. Python 3.6+.
